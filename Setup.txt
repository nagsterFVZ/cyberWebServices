Socket Server
1. Run "npm install"
2. Run "node index.js"

UI Server
1. Run "npm install"
2. Run "npm run start"

For UI Server Build
1. Run "npm run build"
2. Run "serve -s build -l 4000" this runs it on port 4000